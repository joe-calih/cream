import Link from "next/link";
import { Check } from "lucide-react";
import { Button } from "@/components/ui/button";
import { Card, CardContent } from "@/components/ui/card";
import { Badge } from "@/components/ui/badge";

const plans = [{ name: "Free", price: "$0", period: "forever", description: "A simple starting point for individuals exploring better meeting memory.", features: ["10 meetings each month", "Basic transcription", "30-day history"], featured: false }, { name: "Pro", price: "$15", period: "per user / month", description: "A complete workspace for teams that want every conversation to move work forward.", features: ["Unlimited meetings", "Advanced transcription", "Unlimited history", "Integrations", "Priority support"], featured: true }];
export function StandalonePricing() { return <section className="bg-background section-padding-y border-b"><div className="container-padding-x container mx-auto max-w-4xl"><div className="grid gap-5 md:grid-cols-2">{plans.map((plan) => <Card key={plan.name} className={`rounded-xl p-6 shadow-sm md:p-8 ${plan.featured ? "border-primary ring-primary/10 ring-4" : ""}`}><CardContent className="flex h-full flex-col gap-7 p-0"><div className="flex items-center justify-between"><h2 className="text-foreground text-xl font-semibold">{plan.name}</h2>{plan.featured ? <Badge>Recommended</Badge> : null}</div><p className="text-muted-foreground min-h-14 text-sm">{plan.description}</p><div><span className="text-foreground text-4xl font-semibold">{plan.price}</span><span className="text-muted-foreground ml-2 text-sm">{plan.period}</span></div><ul className="flex flex-1 flex-col gap-4">{plan.features.map((feature) => <li key={feature} className="text-muted-foreground flex items-center gap-3 text-sm"><Check className="text-primary size-4" />{feature}</li>)}</ul><Button asChild variant={plan.featured ? "default" : "outline"}><Link href="/login">Choose {plan.name}</Link></Button></CardContent></Card>)}</div></div></section>; }
